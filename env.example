# ============================================
# CRAVEVERSE ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in your actual values
# All features are currently FREE - Stripe payment disabled

# --------------------------------------------
# APP CONFIGURATION
# --------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --------------------------------------------
# CLERK AUTHENTICATION
# --------------------------------------------
# Get these from: https://dashboard.clerk.com
# Required for user authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY=sk_test_YOUR_CLERK_SECRET_KEY
CLERK_WEBHOOK_SECRET=whsec_YOUR_CLERK_WEBHOOK_SECRET

# --------------------------------------------
# SUPABASE DATABASE
# --------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Required for database operations
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
# Optional: Legacy SUPABASE_URL (same as NEXT_PUBLIC_SUPABASE_URL)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# --------------------------------------------
# OPENAI API
# --------------------------------------------
# Get this from: https://platform.openai.com/api-keys
# Required for AI features (GPT-5-mini)
# Token limit: 25,000 tokens per user per day (strictly enforced)
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY

# --------------------------------------------
# REDIS CACHE (Optional)
# --------------------------------------------
# Used for caching AI responses and job queues
# Can be left empty if not using Redis
REDIS_URL=redis://localhost:6379

# --------------------------------------------
# NOTES
# --------------------------------------------
# - All Stripe payment features are DISABLED (all features are free)
# - Token limit: 25,000 tokens/user/day for GPT-5-mini (strictly enforced)
# - If any critical env vars are missing or contain "placeholder", the app runs in MOCK MODE
# - Mock mode allows local development without API keys
