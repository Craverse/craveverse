# =============================================
# CraveVerse Environment Template (.env.example)
# =============================================
# Copy this file to:
#   - .env.local for local development
#   - Hosting provider / Vercel environment settings for production
# Fill in every value marked REQUIRED to avoid fallback mock mode.
# =============================================

# ---------------------------------------------
# Core Runtime
# ---------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---------------------------------------------
# Clerk Authentication (REQUIRED)
# https://dashboard.clerk.com
# ---------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=YOUR_CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY=YOUR_CLERK_SECRET_KEY
CLERK_WEBHOOK_SECRET=YOUR_CLERK_WEBHOOK_SECRET

# ---------------------------------------------
# Supabase Database (REQUIRED)
# https://supabase.com/dashboard/project/<project-ref>/settings/api
# ---------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
# Optional legacy alias referenced by diagnostics
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# ---------------------------------------------
# OpenAI (REQUIRED for AI coaching)
# https://platform.openai.com/api-keys
# ---------------------------------------------
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# ---------------------------------------------
# Optional Integrations
# ---------------------------------------------
# Redis / Vercel KV for caching (leave blank to disable)
KV_URL=YOUR_REDIS_OR_VERCEL_KV_URL

# PostHog analytics (optional, UI fallbacks gracefully if unset)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Admin/debug token for protected diagnostic endpoints (dev only)
ADMIN_TOKEN=local-admin-token

# ---------------------------------------------
# Stripe Billing (Currently disabled; keep blank unless re-enabled)
# https://dashboard.stripe.com/apikeys
# ---------------------------------------------
STRIPE_PUBLISHABLE_KEY=YOUR_STRIPE_PUBLISHABLE_KEY
STRIPE_SECRET_KEY=YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=YOUR_STRIPE_WEBHOOK_SECRET

# ---------------------------------------------
# Notes
# ---------------------------------------------
# - Missing or placeholder values force the app into MOCK MODE
#   (no personalization, no Supabase writes, artificial delays)
# - Required keys: CLERK_*, SUPABASE_*, OPENAI_API_KEY, NEXT_PUBLIC_APP_URL
# - Optional: KV_URL, PostHog, ADMIN_TOKEN, Stripe keys
# - For production, ensure NODE_ENV=production and APP_URL points to your domain